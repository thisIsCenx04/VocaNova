namespace VocaNova.Dashboard;

/// <summary>
/// Marker cho chuỗi localization dùng chung (shared resource).
/// Chuỗi nằm ở Resources/SharedResource.en.resx và Resources/SharedResource.vi.resx.
/// Inject qua <c>IStringLocalizer&lt;SharedResource&gt;</c> (đã khai báo trong _ViewImports là <c>L</c>).
/// </summary>
public sealed class SharedResource
{
}
